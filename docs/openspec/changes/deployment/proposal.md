# Proposal

## 背景

当前阶段最重要的是先把系统跑起来，而且运行方式要尽量简单。

因为前期主要在本地 Windows 机器上使用，所以部署能力不应该一开始就围绕公网、高可用或复杂运维展开。

## 本次要解决的问题

- 前期最简单的交付方式是什么
- 前端、后端和数据库怎么一起启动
- 环境变量怎么统一管理

## 本次范围

- 本地优先的 Docker Compose 结构
- 后端、前端和 MySQL 的容器化运行方式
- 基础环境变量模板

## 暂不包含

- 公网域名与 HTTPS
- 高可用、弹性扩缩容
- 复杂监控告警体系
