# Review R01 — stock-registry

**Status:** open
**Owned Tasks:** T01, T02, T03, T04, T05

## Findings

T01–T04 代码已编写完成，但尚未在 NAS 目标环境执行编译和测试。sandbox 中无 JDK/Maven，无法验证。

## Verification

| Task | 验证项 | 状态 |
|---|---|---|
| T01–T04 | 代码文件全部创建，包结构符合 DDD 分层 | ✅ |
| T05 | NAS 环境 `mvnw test` + `/api/stocks` 冒烟 | ⏳ 待执行 |

**待执行命令：**
```bash
cd backend && ./mvnw test -q
curl http://localhost:8089/api/stocks
curl "http://localhost:8089/api/stocks?name=浦发"
```

## Next

continue:T05
