# Proposal

## 背景

项目已经明确采用“本地优先”的查询方式。

这意味着同步动作必须从查询链路中拆出来，变成一条显式的独立能力，否则系统行为会变得不可控。

## 本次要解决的问题

- 手动同步如何触发
- 多数据源怎么按顺序兜底
- 同步结果如何留痕

## 本次范围

- 按指定代码拉取股票、财务或公告相关数据
- 同步记录与失败记录
- 后续定时同步的预留入口

## 暂不包含

- 查询时自动回源
- 自动修正解析失败的数据
